import Link from "next/link";
import { listPortalCases } from "@/server/services/portal-service";

export default async function ClientPortalHome() {
  const portal = await listPortalCases();
  return <div><p className="text-sm font-medium text-brand-700">Client portal</p><h1 className="mt-1 text-2xl font-semibold">Welcome, {portal.clientName}</h1><p className="mt-2 text-sm text-neutral-600">View your case progress, upload required documents, and message your case team.</p><section className="mt-7 space-y-4">{portal.cases.length ? portal.cases.map((caseItem) => <Link key={caseItem.id} href={`/portal/cases/${caseItem.id}`} className="block rounded-lg border border-neutral-200 bg-white p-5 shadow-panel transition-colors hover:border-brand-300"><div className="flex flex-wrap items-start justify-between gap-3"><div><h2 className="font-semibold text-neutral-900">{caseItem.title}</h2><p className="mt-1 text-sm text-neutral-600">{caseItem.streamLabel}</p></div><span className="rounded-full bg-brand-50 px-2.5 py-1 text-xs font-medium text-brand-700">{caseItem.status.replaceAll("_", " ")}</span></div><div className="mt-5"><div className="flex justify-between text-sm"><span>Checklist progress</span><span>{caseItem.checklistCompleted}/{caseItem.checklistTotal}</span></div><div className="mt-2 h-2 overflow-hidden rounded-full bg-neutral-100"><div className="h-full bg-brand-600" style={{ width: `${caseItem.progressPercent}%` }} /></div></div>{caseItem.nearestDeadline ? <p className="mt-4 text-sm text-warning-700">Next deadline: {new Date(caseItem.nearestDeadline).toLocaleDateString()}</p> : null}</Link>) : <p className="rounded-lg border border-neutral-200 bg-white p-5 text-sm text-neutral-600">No active cases are available in this portal.</p>}</section><p className="mt-8 text-xs text-neutral-500">This portal helps you share information with your authorized representative. It does not provide legal advice.</p></div>;
}
